package com.meiken.format.impl;

import com.meiken.format.FormatHandler;

public class JsonFormatHandler implements FormatHandler {
    public <T> String format(T t) {
        return "JsonFormatHandler:" + JSON.
    }
}
